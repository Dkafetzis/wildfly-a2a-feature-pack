name: Maven

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - '*'
  merge_group:
    types:
      - checks_requested

permissions:
  contents: read

concurrency:
  group: '${{ github.workflow }} @ ${{ github.head_ref || github.ref }}'
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os:
          - ubuntu-latest
        java:
          - 17
          - 21
          - 25
          - 26

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
          cache: maven

      - name: Build and Test with Maven
        run: ./mvnw --batch-mode --no-transfer-progress install

      - uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: ${{ matrix.os }}-${{ matrix.java }}-test-reports
          path: |
            **/surefire-reports/*
            **/failsafe-reports/*

  # Provisions the integration testsuite from a WildFly Glow deployment scan instead of the
  # hardcoded layer lists, verifying the org.wildfly.rule.* metadata in the layer specs.
  glow:
    name: Glow layer discovery
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17
          cache: maven

      - name: Build and Test with Maven
        run: ./mvnw -B -ntp install -Dts.glow

      - uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: glow-test-reports
          path: |
            **/surefire-reports/*
            **/glow-scan/provisioning.xml
